import { connect } from 'react-redux';
import gifSearch from './gif_search';

const mapStateToProps = () => ({

})

const mapDispatchToProps = () => ({

})

export default connect(
  mapStateToProps,
  mapDispatchToProps)(gifSearch);
